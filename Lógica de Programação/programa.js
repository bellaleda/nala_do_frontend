console.log("=== GAROTA DE PROGRAMA ===")

// Entrada (dados): capturar dados so user
//janela q permite entrada de dados. quando executado, devove uma resposta, o que foi digitado, return. const para guardar a info
let nome = prompt("Insira seu nome");

// Processamento - recebe um dado e formata a informação, processa, trabalha 
nome = nome.toUpperCase(); 

// Saída de dados - resultado
document.write(nome);